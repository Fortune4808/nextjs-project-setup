import styles from "@/styles/dashboard.module.css";

const Dashboard = () => {
    return (
        <div className={`${styles.container}`}>
            <div className={`${styles.top} bg-amber-50`}>

            </div>
            <div className={`${styles.bottom} bg-white/90`}>

            </div>
        </div>
    )
}

export default Dashboard
